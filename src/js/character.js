/* eslint-disable no-unused-vars */
/**
 * <Общее описание>
 *
 * @param { string } name - name Character
 * @param { string } type - type Character
 *
 * @throws  Will throw an error if the argument is null/undefined
 */
function Character(name, type) {
  this.name = name;
  this.type = type;
}
